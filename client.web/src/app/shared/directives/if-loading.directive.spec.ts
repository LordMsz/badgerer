import { IfLoadingDirective } from './if-loading.directive';

describe('IfLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new IfLoadingDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
